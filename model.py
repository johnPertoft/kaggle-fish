import tensorflow as tf
import numpy as np

def conv2d(prev, shape, scope_name):
    with tf.variable_scope(scope_name) as scope:
       pass 

class Fishmodel:
    def __init__(self, X):
        # TODO: smart model
        self.foo = tf.Variable(tf.constant(1))

